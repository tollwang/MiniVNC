# 更新日志（Changelog）

本项目的版本变更记录。版本号遵循 `主.次.修订`。

## v1.0.8

补齐 v1.0.7 审计中记录但未修的四项。

- **新增：支持 DesktopSize 伪编码(-223)，Mac 改分辨率不再需要手动重连**。此前不协商此编码，远端分辨率
  一变画面就一直错乱。现在协商后，服务器会在分辨率变化时告知新尺寸，客户端重建帧缓冲与位图、按新区域
  重开连续更新、并请求一次完整刷新；状态栏与分辨率显示同步更新。
- **修复：大写锁定（CapsLock）不参与字母大小写判断**，开着 CapsLock 时远端输入的大小写与本地不一致。
  现按 `Shift XOR CapsLock` 计算（大写锁定时按住 Shift 反而出小写，与本机行为一致）。
- **优化：剪贴板同步改为系统事件驱动**（`WM_CLIPBOARDUPDATE`），不再 500ms 轮询。轮询用的
  `Clipboard.GetText` 是 OLE 调用，别的程序占着剪贴板时可能阻塞数百毫秒，而它跑在渲染所在的 UI 线程上，
  会让远程画面一顿一顿。改为事件驱动后平时零开销；同时取消了 v1.0.7 里"窗口未激活就不轮询"的折中——
  现在本窗口不在前台时的复制也能立刻同步过去。
- **清理：`ClipboardHelper` 去掉每次调用现开一个 STA 线程的死分支**（调用方一直都在 UI 线程），
  非 STA 时改为转交应用 Dispatcher，并删除未使用的 `TryGetText`。

## v1.0.7

一轮完整代码审计后的修复，集中在**断开时的界面冻结**、**网络层开销**与**几处会导致掉线/冻结的健壮性缺口**。

- **修复：断开连接/重连/关窗时界面冻结约 2 秒**。`Disconnect()` 原本先等消息循环退出、再关闭 socket；
  但挂起中的异步 socket 读并不响应 `CancellationToken`，空闲连接上消息循环只能一直阻塞在 `ReadAsync`
  里，于是每次都白等满 2 秒超时。改为**先关流唤醒阻塞读、再等待线程退出**。
- **性能：读侧加 64KB 缓冲**。`NetworkStream` 无缓冲，而 RFB 解析极细粒度（Hextile 每个瓦片/子矩形
  只读 1~4 字节），原来每次读都是一次 recv 系统调用——一帧上万次。加缓冲后压到几十次。
- **性能：客户端消息改为拼装后一次写出**。socket 开了 NoDelay（关 Nagle），原先逐字段写让一条 6 字节的
  鼠标消息变成 **4 个 TCP 包**，更新请求是 6 个。现在每条消息一个包，高频鼠标移动时的系统调用与包数降为 1/4。
- **性能：小读取复用暂存区**，不再为每个像素/坐标/消息类型各分配一个数组（一帧上万次分配）。
- **修复：连续更新被服务器停用后画面永久冻结**。`EndOfContinuousUpdates` 既表示"支持"也用作"已停用"的
  边界标记，原来只处理第一次；若服务器停用，则服务器不再主动推、客户端也不再请求，双方互等，
  TCP 却还活着（keepalive 不会触发重连）。现在再次收到即退回逐帧请求并立刻补发一次。
- **修复：越界矩形不再断开整条会话**。帧缓冲写入/CopyRect 原本对超出边界的矩形抛异常，会被当成协议错误
  断线重连。矩形宽高已用于解码、流并未错位，错的只是落点，改为裁剪——画不下就不画。
- **修复：同一客户端二次连接会用上一条连接的 zlib 上下文解 ZRLE**（花屏或"不支持的子编码"）。
  新增 `IEncoding.ResetState()`，每次初始化会话时清空跨矩形保持的解码状态。
- **修复：主动断开时残留的写操作被报成错误**（正常收尾不再弹错误状态）。
- **优化：会话窗口未激活时不轮询剪贴板**。`Clipboard.GetText` 是 OLE 调用，别的程序占用剪贴板时可能
  阻塞数百毫秒，而它跑在渲染所在的 UI 线程上，会让远程画面一顿一顿。

## v1.0.6

- **新增：主机地址支持 `host:port` 合写**。快速连接与连接编辑框都可直接填
  `mac.example.com:5901`、`[::1]:5901`，端口自动拆分并回写到端口框；裸 IPv6（`::1`）
  照旧整串当地址。解析与校验统一到新的 `Core/HostAddress.cs`。
  （域名本就能用——主机名直接交给 `TcpClient.ConnectAsync` 内部做 DNS 解析。）
- **修复：域名写错要白等约 30 秒才报错**。DNS 解析失败（`HostNotFound`/`NoData` 等）
  原被当作"连接被重置"类瞬时错误，会按退避重试 5 次且提示语误导。现在这类确定性错误
  第一次失败即返回；连接被重置/超时/拒绝的重试行为不变。

## v1.0.5

- **新增：ContinuousUpdates（连续更新）伪编码**。客户端协商 `-313`；服务器支持时回发
  `EndOfContinuousUpdates`，客户端开启后由**服务器主动推送增量帧**，不再逐帧发送
  `FramebufferUpdateRequest`，**省掉每帧一次往返延迟、更跟手**。服务器不支持时自动回退到
  原"请求-应答"循环，零副作用。

## v1.0.4

- **修复：工作一段时间后顶部工具栏（菜单）消失、点击无反馈**。
  - 移除视口的鼠标捕获（`CaptureMouse`）——捕获一旦卡住会让工具栏触发区收不到鼠标、
    还会拦截工具栏按钮点击。改为每次移动从事件参数同步真实按钮状态（自动纠正漏掉的
    按下/松开）、离开视口即释放远端按钮。
  - 消除工具栏 XAML 触发器与代码后台的双重动画打架（会卡在 `Opacity=0` 看着像消失），
    改为确定性显示/隐藏；鼠标停在工具栏上时不自动隐藏，方便点按钮。
- **修复：本地复制内容无法在 Mac 粘贴的一处隐患**。收发两方向原共用 `_isSyncingClipboard`
  标志，macOS 频繁回发剪贴板会把发送方向卡死；改为单一 `_lastClipboard` 值判重，天然
  防回声、无竞态。

## v1.0.3

- **修复：鼠标操作不丝滑、工作几分钟后所有点击无反馈（卡死）**。
  - 输入写入改为**后台写线程 + 写队列**：UI 线程只入队不阻塞，避免发送缓冲填满时
    同步写阻塞 UI 线程导致界面冻结。
  - socket 增加 **SendTimeout(5s) + TCP keepalive(15/5/3s)**：半死连接约 30 秒内被发现 →
    触发自动重连恢复。
  - 鼠标移动**按远程像素+按钮状态去重**，减少洪泛、更跟手。

## v1.0.2

- **修复：退出后立即重连被 macOS 重置**（`远程主机强迫关闭了一个现有的连接`）。
  退出后过快重连时 macOS 上一会话（screensharingd）尚未释放会 RST。连接序列改为
  最多 5 次、递增退避（约 3/5/8/11 秒，共 ~27 秒）的重试，不猛连，覆盖释放窗口；
  认证失败/超时不重试。

## v1.0.1

- **新增：Apple/ARD 认证（安全类型 30）**——macOS 屏幕共享默认认证，支持用户名+密码
  （DH + MD5 + AES-128-ECB）。
- **新增：远端光标本地渲染**（Cursor 伪编码 -239），鼠标更跟手、无重影。
- **新增：剪贴板 UTF-8 自适应编码**，改善 Mac↔Windows 中文/Emoji 剪贴板。
- **增量刷新**：只拷贝/重绘变化矩形，大幅提升流畅度；合并式 `BeginInvoke` 渲染。
- **韧性**：断线自动重连（含快速掉线保护）、仅查看模式、画质（16/32 位色）选项。
- **深度多智能体审计修复**一批输入/编码/安全问题：Alt/Option 键、Shift 先松导致远端卡键、
  Hextile 彩色子矩形颜色泄漏、DES/DPAPI 密码加解密损坏、连接失败 NRE、整数溢出防护、
  配置原子写等。
- 主窗口标题栏显示版本号。

## v1.0.0

- 首个可用版本：从无法编译的初始代码完成整体修复与重写，可在真实 Windows 上构建运行。
- VNC 客户端核心：RFB 3.8 握手、VNC 密码(DES)/无认证、初始化协商。
- 编码：Raw、Hextile、CopyRect、ZRLE（帧缓冲统一 BGRA32）。
- 连接管理器（连接簿、快速连接、DPAPI 加密保存密码）、远程会话窗口（全屏/缩放、
  Mac 快捷键菜单、文本剪贴板同步）。
- 发布：win-x64 单文件自包含 `MiniVNC.exe`，目标机无需预装 .NET 运行时。

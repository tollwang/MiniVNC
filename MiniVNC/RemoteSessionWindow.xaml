<Window x:Class="MiniVNC.RemoteSessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrls="clr-namespace:MiniVNC.Controls"
        mc:Ignorable="d"
        Title="MiniVNC - 远程会话"
        WindowStyle="None"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Background="#0E0E0E"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <!-- 工具栏按钮样式 -->
        <Style x:Key="ToolButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E42"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 危险操作按钮样式 -->
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolButtonStyle}">
            <Setter Property="Background" Value="#C75450"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#C75450"/>
        </Style>

        <!-- 状态栏文本 -->
        <Style x:Key="StatusBarTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#808080"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>

        <!-- 缩放模式选择样式 -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- VNC渲染控件 - 填充整个窗口 -->
        <ctrls:VncViewport x:Name="VncViewport"
                           StretchMode="Fit"
                           Focusable="True"
                           FocusVisualStyle="{x:Null}"/>

        <!-- 悬浮工具栏 - 顶部，默认隐藏 -->
        <Border x:Name="ToolbarPanel"
                Background="#CC1E1E1E"
                BorderBrush="#3E3E42"
                BorderThickness="0,0,0,1"
                Padding="8,6"
                VerticalAlignment="Top"
                Opacity="0"
                Visibility="Collapsed">
            <Border.Triggers>
                <EventTrigger RoutedEvent="Border.MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             To="1"
                                             Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="Border.MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             To="0"
                                             Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
            <StackPanel Orientation="Horizontal">
                <!-- 返回窗口模式 -->
                <Button x:Name="BtnWindowed"
                        Style="{StaticResource ToolButtonStyle}"
                        Click="BtnWindowed_Click"
                        ToolTip="返回窗口模式 (Ctrl+Alt+W)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE73F;"
                                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   Margin="0,0,4,0"/>
                        <TextBlock Text="窗口"/>
                    </StackPanel>
                </Button>

                <!-- 全屏切换 -->
                <Button x:Name="BtnFullscreen"
                        Style="{StaticResource ToolButtonStyle}"
                        Click="BtnFullscreen_Click"
                        ToolTip="切换全屏 (Ctrl+Alt+F)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE740;"
                                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   Margin="0,0,4,0"/>
                        <TextBlock Text="全屏"/>
                    </StackPanel>
                </Button>

                <Border Width="1"
                        Background="#3E3E42"
                        Margin="6,0"/>

                <!-- 缩放模式 -->
                <ComboBox x:Name="CmbZoomMode"
                          Style="{StaticResource DarkComboBoxStyle}"
                          Width="90"
                          SelectionChanged="CmbZoomMode_SelectionChanged"
                          ToolTip="缩放模式">
                    <ComboBoxItem Content="100%" Tag="Original"/>
                    <ComboBoxItem Content="自适应" Tag="Fit" IsSelected="True"/>
                    <ComboBoxItem Content="拉伸" Tag="Stretch"/>
                </ComboBox>

                <Border Width="1"
                        Background="#3E3E42"
                        Margin="6,0"/>

                <!-- 发送Mac快捷键 -->
                <Button x:Name="BtnSendKey"
                        Style="{StaticResource ToolButtonStyle}"
                        Click="BtnSendKey_Click"
                        ToolTip="发送Mac快捷键">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE765;"
                                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   Margin="0,0,4,0"/>
                        <TextBlock Text="快捷键"/>
                    </StackPanel>
                </Button>

                <!-- 剪贴板同步 -->
                <Button x:Name="BtnClipboard"
                        Style="{StaticResource ToolButtonStyle}"
                        Click="BtnClipboard_Click"
                        ToolTip="剪贴板同步">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE77F;"
                                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   Margin="0,0,4,0"/>
                        <TextBlock Text="剪贴板"/>
                    </StackPanel>
                </Button>

                <Border Width="1"
                        Background="#3E3E42"
                        Margin="6,0"/>

                <!-- 断开连接 -->
                <Button x:Name="BtnDisconnect"
                        Style="{StaticResource DangerButtonStyle}"
                        Click="BtnDisconnect_Click"
                        ToolTip="断开连接 (Ctrl+Alt+D)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE71A;"
                                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   Margin="0,0,4,0"/>
                        <TextBlock Text="断开"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- 工具栏触发区域 - 鼠标靠近顶部时显示工具栏 -->
        <Border x:Name="ToolbarTrigger"
                Background="Transparent"
                Height="4"
                VerticalAlignment="Top"
                MouseEnter="ToolbarTrigger_MouseEnter"
                MouseLeave="ToolbarTrigger_MouseLeave"/>

        <!-- 底部状态栏 -->
        <Border x:Name="StatusBar"
                Background="#CC1E1E1E"
                BorderBrush="#3E3E42"
                BorderThickness="0,1,0,0"
                Padding="8,4"
                VerticalAlignment="Bottom">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TbStatusInfo"
                           Grid.Column="0"
                           Style="{StaticResource StatusBarTextStyle}"
                           Text="正在连接..."/>
                <TextBlock x:Name="TbResolution"
                           Grid.Column="1"
                           Style="{StaticResource StatusBarTextStyle}"
                           Margin="12,0,0,0"/>
                <TextBlock x:Name="TbEncoding"
                           Grid.Column="2"
                           Style="{StaticResource StatusBarTextStyle}"
                           Margin="12,0,0,0"/>
                <TextBlock x:Name="TbConnectionState"
                           Grid.Column="3"
                           Style="{StaticResource StatusBarTextStyle}"
                           Foreground="#107C10"
                           Margin="12,0,0,0"
                           Text="已连接"/>
            </Grid>
        </Border>
    </Grid>
</Window>
